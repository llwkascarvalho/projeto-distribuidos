# Versão 2 — Monólito Modular

Sistema de pedidos de lanchonete com **fronteiras explícitas entre módulos**,
comunicação via interfaces tipadas e schemas separados por domínio no banco.

## Como rodar

```bash
docker compose up --build